# docs/

FormalTask documentation.

| Directory | Content |
| --- | --- |
| `getting-started/` | Installation and quickstart |
| `cli/` | CLI command reference (`ft task`, `ft epic`, etc.) |
| `architecture/` | System design docs (overview, formulas, worktrees, skill spans) |

Adding a doc: put it in the right directory, link it from the parent `index.md` or `overview.md`.
