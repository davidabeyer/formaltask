[pytest]
# TDD Guard configuration for this worktree
# Each worktree has isolated test state for parallel agent execution
tdd_guard_project_root = .

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
